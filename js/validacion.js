const campoNombre = document.querySelect ("name=nombre");
const campoPais = document.querySelect ("name=pais");
const campoZipCode = document.querySelect ("name=zipCode");
const campoEmail = document.querySelect ("name=email");
const campoTelefono = document.querySelect ("name=telefono");

campoNombre.addEventListener ("blur", function (e){
    const campo = e.target;
    const valorCampo = e.target.value;
    if (valorCampo.length == 0) {
        FileSystemDirectoryHandle.parentElement.insertAdjacentHTML("beforeend",
        
        )
        
    }
})


